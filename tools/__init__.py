"""Local tools the agent can call and iterate against (SIH problem statement:
"call local tools such as file read and write, code execution in a sandbox,
spreadsheet work, internal document search, and iterate on a task"). Starts
with tools/sandbox.py (code execution); more tools land here the same way.
"""
